# 文档模板集 — product-doc-lifecycle v3.1

## 使用说明

复制对应模板到目标路径，替换 `{占位符}` 后使用。

**命名规范（v2.1）**：所有文档文件名使用 kebab-case，无例外。
- 核心文档：`{序号}-{英文名}.md`（如 `01-requirements.md`），文件名固定，不含版本号
- 子项目文档：`{子项目ID}-{序号}-{英文名}.md`（如 `admin-01-requirements.md`），文件名固定，不含版本号
- API 规范：`docs/api/openapi.yaml`（机器可读 OpenAPI 规范）
- **版本号在文件内容头部维护**，不体现在文件名中

---

## README.md 模板

```markdown
# {项目名} — {一句话描述}

## 项目简介

{2-3 句描述核心价值}

### 核心特性

- {特性1}
- {特性2}

## 快速开始

### 前置要求

- OS: {操作系统}
- 内存: {最低内存}
- 依赖: {主要依赖}

### 一键启动

```bash
{启动命令}
```

## 技术架构

```
{ASCII 架构图}
```

| 分类 | 技术 |
|------|------|
| 后端 | {技术} |
| 前端 | {技术} |
| 数据库 | {技术} |

## 文档体系

完整文档索引：[docs/DOCUMENTATION_INDEX.md](docs/DOCUMENTATION_INDEX.md)

| # | 文档 | 描述 |
|---|------|------|
| 1 | [需求规格](docs/core/01-requirements.md) | 功能需求定义 |
| 2 | [系统架构](docs/core/02-architecture.md) | 架构与技术选型 |
| 3 | [数据库设计](docs/core/03-database.md) | 表结构与关系 |
| 4 | [API 文档](docs/core/04-api.md) | 接口规范 |
| 5 | [功能指南](docs/core/05-feature-guide.md) | 使用说明 |
| 6 | [部署手册](docs/core/06-deployment.md) | 部署与运维 |
| 7 | [版本说明](docs/core/07-release-notes.md) | 发布历史 |
| 8 | [使用说明](docs/core/08-user-guide.md) | 用户使用手册（如有终端用户） |

## 版本历史

| 版本 | 发布日期 | 主要变更 |
|------|---------|---------|
| V{ver} | {日期} | 初始版本 |

---

**版本**: V{ver} | **最后更新**: {日期} | **状态**: {开发中/稳定}
```

---

## CHANGELOG.md 模板

```markdown
# Changelog

All notable changes to this project will be documented here.

Format: [Keep a Changelog](https://keepachangelog.com/) + Conventional Commits

## [Unreleased]

## [{ver}] - {日期}

### Added
-

### Fixed
-

### Changed
-

### Breaking Changes
- 无
```

---

## docs/DOCUMENTATION_INDEX.md 模板

```markdown
# 文档索引

> 最后更新: {日期} | 核心版本: V{ver}

## 核心文档

| # | 文档名称 | 版本 | 路径 | 最后更新 |
|---|---------|------|------|---------|
| 1 | 需求规格说明书 | V{ver} | [链接](core/01-requirements.md) | {日期} |
| 2 | 系统架构设计   | V{ver} | [链接](core/02-architecture.md) | {日期} |
| 3 | 数据库设计     | V{ver} | [链接](core/03-database.md) | {日期} |
| 4 | API 接口文档   | V{ver} | [链接](core/04-api.md) | {日期} |
| 5 | 功能指南       | V{ver} | [链接](core/05-feature-guide.md) | {日期} |
| 6 | 部署与运维手册 | V{ver} | [链接](core/06-deployment.md) | {日期} |
| 7 | 版本发布说明   | V{ver} | [链接](core/07-release-notes.md) | {日期} |
| 8 | 产品使用说明   | V{ver} | [链接](core/08-user-guide.md) | {日期} |

## 子项目文档

### {子项目名} — {一句话描述}

| # | 文档名称 | 版本 | 路径 | 状态 |
|---|---------|------|------|------|
| 1 | 功能需求 | V{ver} | [链接]({subproject}/{subproject}-01-requirements.md) | 开发中 |
| 2 | 功能架构设计 | V{ver} | [链接]({subproject}/{subproject}-02-architecture.md) | 开发中 |
| 3 | 数据模型 | V{ver} | [链接]({subproject}/{subproject}-03-data-model.md) | 开发中 |
| 4 | API 文档 | V{ver} | [链接]({subproject}/{subproject}-04-api.md) | 开发中 |
| 5 | 功能指南 | V{ver} | [链接]({subproject}/{subproject}-05-feature-guide.md) | - |
| 6 | UI/交互设计 | V{ver} | [链接]({subproject}/{subproject}-06-ui-design.md) | - |
| 7 | 部署说明 | V{ver} | [链接]({subproject}/{subproject}-07-deployment.md) | - |

## 部署专题

| # | 文档名称 | 版本 | 路径 |
|---|---------|------|------|
| - | （待添加） | - | - |

## 归档子项目

| 子项目 | 归档日期 | 归档版本 | 说明 |
|-------|---------|---------|------|
| - | - | - | - |

## 归档文档

| 文档名称 | 归档版本 | 归档日期 |
|---------|---------|---------|
| - | - | - |

---

**按角色查阅**

| 角色 | 推荐阅读 |
|------|---------|
| 产品经理 | README → 01-requirements → 07-release-notes |
| 架构师   | README → 02-architecture → 03-database |
| 后端开发 | README → 02-architecture → 04-api → 03-database |
| 前端开发 | README → 04-api → 05-feature-guide → 子项目文档 |
| QA       | README → 01-requirements → 04-api → 05-feature-guide |
| DevOps   | 06-deployment → 02-architecture → 部署专题 |
| AI Agent | README → 02-architecture → 04-api → 03-database（按需其他） |
```

---

## 核心文档模板

放置路径：`docs/core/{序号}-{英文名}.md`

### 01-requirements 模板

```markdown
# 需求规格说明书

> **版本**: V{ver} | **最后更新**: {日期} | **状态**: {草稿/评审中/已冻结} | **负责人**: {姓名}

## 1. 项目背景

{描述项目背景和业务驱动力}

## 2. 技术目标

| 指标 | 目标值 |
|------|--------|
| 响应时间 | < {N}ms |
| 并发量 | {N} QPS |
| 可用性 | {99.9%} |

## 3. 功能需求

### 3.1 {模块名}

**功能描述**：{描述}

**用户故事**：
- 作为 {角色}，我希望 {功能}，以便 {目的}

**验收标准**：
- [ ] {可测试条件 1}
- [ ] {可测试条件 2}

### 3.2 {模块名}

...

## 4. 非功能性需求

### 4.1 性能要求
### 4.2 安全要求
### 4.3 兼容性要求

## 5. 范围边界（Out of Scope）

- {明确不做的事项 1}
- {明确不做的事项 2}

---

**最后更新**: {日期} | **状态**: {草稿/评审中/已冻结} | **负责人**: {姓名}
```

```markdown
# 系统架构设计

> **版本**: V{ver} | **最后更新**: {日期} | **负责人**: {姓名}

## 1. 架构总览

```
{系统组件图 - Mermaid 或 ASCII}
```

## 2. 服务分层

| 层级 | 职责 | 技术 |
|------|------|------|
| 前端层 | 用户界面 | {技术} |
| API 网关 | 路由、鉴权、限流 | {技术} |
| 业务服务层 | 核心业务逻辑 | {技术} |
| 数据层 | 数据持久化 | {技术} |

## 3. 端口规划

| 端口 | 服务 | 用途 |
|------|------|------|
| {端口} | {服务名} | {用途} |

## 4. 技术栈

| 分类 | 技术 | 版本 | 选型原因 |
|------|------|------|---------|
| 后端框架 | {技术} | {版本} | {原因} |
| 前端框架 | {技术} | {版本} | {原因} |
| 数据库 | {技术} | {版本} | {原因} |

## 5. 数据流

{描述请求如何流经各服务}

```
客户端 → API 网关 → 业务服务 → 数据库
```

## 6. 设计决策记录（ADR）

| 决策 | 方案 | 原因 | 日期 |
|------|------|------|------|
| {决策标题} | {选择方案} | {选择原因} | {日期} |

---

**最后更新**: {日期} | **负责人**: {姓名}
```

### 03-database 模板

```markdown
# 数据库设计

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 数据库规范

- 数据库类型：{类型} {版本}
- 命名规则：{表名/字段名规则}
- 字符集：{utf8mb4 / 其他}

## 2. 核心表

### {table_name}

**说明**：{表的用途}

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | - | 主键 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 更新时间 |

### {table_name}

...

## 3. 索引

| 表名 | 索引名 | 字段 | 类型 | 说明 |
|------|--------|------|------|------|
| {table} | idx_{name} | {字段} | BTREE | {用途} |

## 4. 表关系

```
{ER 图描述或 Mermaid erDiagram}
```

## 5. 迁移记录

| 版本 | 变更内容 | 日期 | 执行人 |
|------|---------|------|--------|
| V{ver} | {变更描述} | {日期} | {姓名} |

---

**最后更新**: {日期}
```

### 04-api 模板

```markdown
# API 接口文档

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 基础信息

- Base URL: `{环境 URL}`
- 认证方式: Bearer Token (JWT)
- Content-Type: application/json

## 2. 通用响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

## 3. 接口列表

### {模块名}

#### {METHOD} {/path}

**描述**：{接口说明}

**请求头**：
```
Authorization: Bearer {token}
Content-Type: application/json
```

**请求体**：
```json
{
  "field": "type"
}
```

**响应**：
```json
{
  "code": 200,
  "data": {}
}
```

## 4. 错误码

| 错误码 | HTTP 状态 | 说明 |
|-------|---------|------|
| 200 | 200 | 成功 |
| 400 | 400 | 请求参数错误 |
| 401 | 401 | 未认证 |
| 403 | 403 | 无权限 |
| 404 | 404 | 资源不存在 |
| 500 | 500 | 服务器内部错误 |

---

**最后更新**: {日期}
```

### 05-feature-guide 模板

```markdown
# 功能指南

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 功能模块概览

| 模块 | 说明 | 状态 |
|------|------|------|
| {模块名} | {说明} | {已完成/开发中/计划中} |

## 2. {功能模块名}

### 2.1 使用说明

{步骤说明}

### 2.2 配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| {配置} | {值} | {说明} |

### 2.3 使用场景

**场景 1：{场景名}**
{描述和步骤}

## 3. 常见问题（FAQ）

**Q: {问题}**
A: {解答}

---

**最后更新**: {日期}
```

### 06-deployment 模板

```markdown
# 部署与运维手册

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 环境要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| OS | {系统} | {系统} |
| CPU | {N} 核 | {N} 核 |
| 内存 | {N} GB | {N} GB |
| 磁盘 | {N} GB | {N} GB |

## 2. 依赖安装

```bash
{安装命令}
```

## 3. 配置项

### 环境变量

| 变量名 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| {变量} | 是 | {值} | {说明} |

### 配置文件

| 文件 | 说明 |
|------|------|
| {文件路径} | {说明} |

## 4. 安装步骤

```bash
# 1. {步骤}
{命令}

# 2. {步骤}
{命令}

# 3. 启动
{启动命令}
```

## 5. 运维命令

| 命令 | 用途 |
|------|------|
| {命令} | {用途} |

## 6. 健康检查

```bash
curl {health-check-url}
```

## 7. 故障排查

| 症状 | 可能原因 | 解决方案 |
|------|---------|---------|
| {症状} | {原因} | {方案} |

## 8. 备份与恢复

### 备份
```bash
{备份命令}
```

### 恢复
```bash
{恢复命令}
```

---

**最后更新**: {日期}
```

### 07-release-notes 模板

```markdown
# 版本发布说明

> **版本**: V{ver} | **发布日期**: {日期}

## 发布信息

- **版本号**：V{ver}
- **发布日期**：{日期}
- **发布负责人**：{姓名}

## 新增功能

- {功能 1 描述}
- {功能 2 描述}

## Bug 修复

- {修复 1 描述}
- {修复 2 描述}

## 变更说明

- {变更 1 描述}

## 破坏性变更（Breaking Changes）

- {破坏性变更描述及影响范围}

## 升级指引

从 V{上一版本} 升级到 V{当前版本}：

1. {步骤 1}
2. {步骤 2}

---

**发布日期**: {日期}
```

---

### 08-user-guide 模板

> **文件名**: `docs/core/08-user-guide.md`
>
> **适用**: 有终端用户的产品（Web UI / 移动端 / 桌面端）
>
> **不适用的项目**: 纯 API 服务、库/框架（用 README + 04-api 代替）

```markdown
# {产品名} 使用说明

> **版本**: V{ver} | **最后更新**: {日期} | **适用产品版本**: V{ver}

## 1. 产品简介

{用 2-3 句话描述产品是什么、能帮你做什么}

**适用人群**：{谁应该用这个产品}

## 2. 快速开始

> 第一次使用？只需完成以下步骤即可上手。

1. {步骤 1：访问地址 / 下载安装}
2. {步骤 2：登录 / 注册}
3. {步骤 3：完成第一个操作，如"创建第一条记录"}

## 3. 功能使用说明

### 3.1 {功能模块名}

**用途**：{用户用这个功能能完成什么}

**操作步骤**：
1. {步骤 1：找到入口}
2. {步骤 2：填写必要信息}
3. {步骤 3：点击确认，查看结果}

**小提示**：{技巧或注意事项}

### 3.2 {功能模块名}

...

## 4. 常见问题（FAQ）

**Q: {用户常问问题}**
A: {用用户语言回答，不用技术术语}

**Q: {另一个问题}**
A: {回答}

## 5. 联系我们

- 反馈问题：{邮箱 / 工单系统 / 应用内反馈入口}
- 查看版本更新：[{产品名} 更新日志]({链接到 07-release-notes 或更新公告})

---

**适用产品版本**: V{ver} | **最后更新**: {日期}
```

---

## 子项目文档模板集

放置路径：`docs/{subproject}/{subproject}-{序号}-{英文名}.md`

---

### 子项目功能需求模板

```markdown
# {子项目名} 功能需求

> **版本**: V{ver} | **最后更新**: {日期} | **状态**: {草稿/已冻结}

## 1. 子项目定位

（在整体系统中的角色，参考 [核心 02-architecture](../core/02-architecture.md)）

## 2. 功能模块

### 2.1 {模块名}

**功能描述**：{描述}

**用户故事**：
- 作为 {角色}，我希望 {功能}，以便 {目的}

**验收标准**：
- [ ] {可测试条件}
- [ ] {可测试条件}

### 2.2 {模块名}

...

## 3. 非功能性需求

| 类别 | 要求 |
|------|------|
| 性能 | {要求} |
| 安全 | {要求} |

## 4. 外部依赖

| 依赖服务/接口 | 来源 | 用途 |
|--------------|------|------|
| {服务名} | [核心 04-api](../core/04-api.md) | {用途} |

## 5. Out of Scope（本子项目不负责的）

- {不在范围内的功能 1}
- {不在范围内的功能 2}

---

**最后更新**: {日期} | **版本**: V{ver} | **状态**: {草稿/已冻结}
```

---

### 子项目功能架构设计模板

```markdown
# {子项目名} 功能架构设计

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 子项目定位

（在整体系统架构中的角色，参考 [核心 02-architecture](../core/02-architecture.md)）

## 2. 内部模块划分

```
{子项目名}/
├── {模块A}/   ← {职责说明}
├── {模块B}/   ← {职责说明}
└── {模块C}/   ← {职责说明}
```

## 3. 技术栈

| 分类 | 技术 | 版本 | 选型原因 |
|------|------|------|---------|
| {分类} | {技术} | {版本} | {原因} |

## 4. 内部数据流

（模块间数据如何流转，可用 Mermaid 或 ASCII 流程图）

## 5. 外部依赖

| 依赖服务 | 调用方式 | 用途 |
|---------|---------|------|
| API Gateway (:8007) | HTTP REST | 数据读写 |

## 6. 设计决策记录（ADR）

| 决策 | 方案 | 原因 |
|------|------|------|
| {决策} | {方案} | {原因} |
```

**前端子项目补充章节**：

```markdown
## 7. 路由结构

| 路径 | 组件 | 说明 | 权限 |
|------|------|------|------|
| /{path} | {Component} | {说明} | {角色} |

## 8. 状态管理（Store 设计）

| Store | 职责 | 主要 State | 持久化 |
|-------|------|-----------|--------|
| {store} | {职责} | {state} | {是/否} |

## 9. 主要组件树

（核心页面组件层级）
```

**后端/微服务子项目补充章节**：

```markdown
## 7. 服务内部分层

| 层级 | 职责 | 示例 |
|------|------|------|
| Controller | 请求处理、参数校验 | UserController |
| Service | 业务逻辑 | UserService |
| Repository | 数据访问 | UserRepository |

## 8. 消息队列/事件设计（如有）

| 事件 | 生产者 | 消费者 | 说明 |
|------|--------|--------|------|
| {事件} | {服务} | {服务} | {说明} |
```

```markdown
---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目数据模型模板（后端/数据库类）

```markdown
# {子项目名} 数据模型

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 概述

本子项目的专属数据表（整体数据库架构参见 [核心 03-database](../core/03-database.md)）。

## 2. 专属表结构

### {table_name}

**说明**：{表的用途}

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | - | 主键 |
| {field} | {type} | {约束} | {默认值} | {说明} |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### {table_name}

...

## 3. 与核心表的关系

| 本子项目表 | 关联字段 | 核心表 | 关系 |
|-----------|---------|--------|------|
| {table} | {field} | {core_table} | N:1 |

## 4. 索引设计

| 表名 | 索引名 | 字段 | 类型 | 说明 |
|------|--------|------|------|------|
| {table} | idx_{name} | {字段} | BTREE | {用途} |

## 5. 迁移记录

| 版本 | 变更内容 | 日期 |
|------|---------|------|
| V{ver} | {变更描述} | {日期} |

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目数据模型模板（前端 Store）

```markdown
# {子项目名} 数据模型（前端 Store）

> **版本**: V{ver} | **最后更新**: {日期}

## 1. Pinia Store 设计

| Store 文件 | 职责 | 持久化 | 初始状态 |
|-----------|------|--------|---------|
| `{name}.store.ts` | {职责} | {localStorage/sessionStorage/否} | {描述} |

## 2. 核心类型定义

```typescript
// types/{module}.ts

export interface {TypeName} {
  id: number
  // ...
}
```

### {TypeName}

| 字段 | 类型 | 说明 |
|------|------|------|
| {field} | {type} | {说明} |

## 3. Store 详细设计

### {store-name}.store.ts

**State**：
```typescript
interface {StoreName}State {
  {field}: {type}
}
```

**Actions**：
| Action | 说明 | 调用的 API |
|--------|------|-----------|
| {action} | {说明} | [接口描述](../core/04-api.md#section) |

**Getters**：
| Getter | 返回类型 | 说明 |
|--------|---------|------|
| {getter} | {type} | {说明} |

## 4. 本地缓存策略

| 数据 | 存储位置 | 过期策略 | 清除时机 |
|------|---------|---------|---------|
| {数据} | {localStorage/key} | {策略} | {时机} |

## 5. 数据流向

```
用户操作 → Component → Store Action → API 请求 → Store State → Component 响应式更新
```

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目 API 文档模板（后端/微服务 — 对外暴露接口）

```markdown
# {子项目名} API 文档

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 基础信息

- Base URL: `{路由前缀}`
- 认证: 同全局规范，参见 [核心 04-api](../core/04-api.md)
- Content-Type: application/json

## 2. 通用响应格式

同 [核心 04-api §通用响应格式](../core/04-api.md#通用响应格式)

## 3. 接口列表

### {功能模块}

#### {HTTP_METHOD} {/path}

**描述**：{接口说明}

**请求头**：
```
Authorization: Bearer {token}
```

**请求体**：
```json
{
  "field": "type"
}
```

**响应**：
```json
{
  "code": 200,
  "data": {}
}
```

## 4. 子项目专属错误码

| 错误码 | HTTP 状态 | 说明 |
|-------|---------|------|
| {code} | {status} | {说明} |

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目 API 消费规范模板（前端子项目）

```markdown
# {子项目名} API 消费规范

> **版本**: V{ver} | **最后更新**: {日期}

## 1. API 客户端目录结构

```
src/api/
├── {module}.ts   ← {模块}相关接口封装
└── index.ts      ← 统一导出
```

## 2. 请求约定

- Base URL: 通过环境变量 `{变量名}` 注入
- Token 注入: 请求拦截器自动附加 Authorization 头
- 超时设置: {N}ms
- 错误拦截:
  - 401 → 跳转登录页
  - 403 → 提示无权限
  - 500 → 全局 toast 提示

## 3. API 封装示例

```typescript
// src/api/{module}.ts
import request from '@/utils/request'

export function getList(params: { page: number; size: number }) {
  return request.get('/api/{path}', { params })
}
```

## 4. 错误处理约定

| 错误类型 | 处理方式 |
|---------|---------|
| 网络错误 | {处理方式} |
| 401 未认证 | {处理方式} |
| 业务错误（code != 200） | {处理方式} |

## 5. 消费的后端接口清单

| 接口 | 来源文档 | 本地封装函数 | 用途 |
|------|---------|------------|------|
| GET /api/{path} | [核心 04-api §{模块}](../core/04-api.md#模块名) | `{function}()` | {用途} |

## 6. Mock 数据策略（开发阶段）

| 工具 | 配置位置 | 说明 |
|------|---------|------|
| {MSW/json-server} | {路径} | {说明} |

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目功能指南模板

```markdown
# {子项目名} 功能指南

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 功能概览

| 功能模块 | 说明 | 入口 |
|---------|------|------|
| {模块} | {说明} | {路径/位置} |

## 2. {功能模块名}

### 2.1 功能说明

{描述该功能做什么、面向谁}

### 2.2 操作步骤

**场景：{场景名}**

1. {步骤 1}
2. {步骤 2}
3. {步骤 3}

### 2.3 配置说明

| 配置项 | 位置 | 可选值 | 说明 |
|--------|------|--------|------|
| {配置} | {位置} | {值} | {说明} |

### 2.4 注意事项

- {注意 1}
- {注意 2}

## 3. 其他功能模块

（按相同结构依次列出）

## 4. 常见问题（FAQ）

**Q: {问题}**
A: {解答}

**Q: {问题}**
A: {解答}

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

### 子项目 UI/交互设计模板（纯前端专用）

```markdown
# {子项目名} UI/交互设计

> **版本**: V{ver} | **最后更新**: {日期} | **状态**: {草稿/已冻结}

## 1. 页面清单

| 路由路径 | 页面名称 | 组件 | 说明 |
|---------|---------|------|------|
| /{path} | {页面名} | {Component} | {说明} |

## 2. 全局布局

```
┌─────────────────────────────────────┐
│              顶部导航栏              │
├────────┬────────────────────────────┤
│        │                            │
│ 侧边栏  │       主内容区             │
│        │                            │
│        │                            │
└────────┴────────────────────────────┘
```

## 3. 核心页面设计

### 3.1 {页面名}

**路由**：`/{path}`
**权限**：{角色要求}

**页面布局**：

```
┌──────────────────────┐
│    页面标题 + 操作栏    │
├──────────────────────┤
│                      │
│    筛选条件区          │
│                      │
├──────────────────────┤
│                      │
│    数据表格 / 卡片     │
│                      │
├──────────────────────┤
│    分页器             │
└──────────────────────┘
```

**关键交互**：
| 交互 | 触发 | 行为 |
|------|------|------|
| {交互名} | {触发条件} | {行为描述} |

### 3.2 {页面名}

...

## 4. 表单校验规则

| 字段 | 规则 | 错误提示 |
|------|------|---------|
| {字段} | {规则} | {提示} |

## 5. 状态与反馈

| 状态 | 视觉表现 | 触发场景 |
|------|---------|---------|
| 加载中 | Skeleton / Spinner | 请求数据时 |
| 空状态 | 空状态插图 + 提示 | 无数据时 |
| 错误 | 错误提示 + 重试按钮 | 请求失败时 |
| 成功 | Toast 提示 | 操作成功时 |

## 6. 设计规范引用

| 规范 | 来源 |
|------|------|
| 色彩 | {设计系统/链接} |
| 字体 | {设计系统/链接} |
| 组件库 | {如 Ant Design / Element Plus} |
| 图标 | {图标库} |

---

**最后更新**: {日期} | **版本**: V{ver} | **状态**: {草稿/已冻结}
```

---

### 子项目部署说明模板

```markdown
# {子项目名} 部署说明

> **版本**: V{ver} | **最后更新**: {日期}

## 1. 概述

本文档描述 {子项目名} 的独立部署流程。
全局基础设施配置参见 [核心 06-deployment](../core/06-deployment.md)。

## 2. 子项目专属环境要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| OS | {系统} | {系统} |
| 内存 | {N} GB | {N} GB |
| 磁盘 | {N} GB | {N} GB |

## 3. 构建步骤

```bash
# 1. 安装依赖
{安装命令}

# 2. 构建
{构建命令}

# 3. 产物目录
{产物路径}
```

## 4. 子项目专属配置

### 环境变量

| 变量名 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| {变量} | 是 | {值} | {说明} |

### 配置文件

| 文件 | 说明 |
|------|------|
| {文件路径} | {说明} |

## 5. 部署步骤

```bash
# 1. {步骤}
{命令}

# 2. {步骤}
{命令}
```

## 6. 健康检查

```bash
curl {health-check-url}
```

**预期响应**：
```json
{ "status": "ok" }
```

## 7. 与核心服务的依赖

| 依赖服务 | 用途 | 检查方式 |
|---------|------|---------|
| {服务} | {用途} | {检查命令} |

## 8. 故障排查

| 症状 | 可能原因 | 解决方案 |
|------|---------|---------|
| {症状} | {原因} | {方案} |

---

**最后更新**: {日期} | **版本**: V{ver}
```

---

## API 规范文件模板

放置路径：`docs/api/openapi.yaml`

```yaml
openapi: "3.0.3"
info:
  title: "{项目名} API"
  description: "{项目描述}"
  version: "{主}.{次}.{补丁}"
  contact:
    name: "{负责人}"

servers:
  - url: "http://localhost:{端口}"
    description: "本地开发环境"
  - url: "https://{域名}"
    description: "生产环境"

security:
  - BearerAuth: []

tags:
  - name: "{模块名}"
    description: "{模块描述}"

paths:
  /api/{path}:
    get:
      tags:
        - "{模块名}"
      summary: "{接口摘要}"
      description: "{接口详细描述}"
      operationId: "get{Resource}List"
      parameters:
        - name: "page"
          in: "query"
          schema:
            type: integer
            default: 1
        - name: "size"
          in: "query"
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: "成功"
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: "success"
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    Unauthorized:
      description: "未认证"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalServerError:
      description: "服务器内部错误"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  schemas:
    ErrorResponse:
      type: object
      properties:
        code:
          type: integer
        message:
          type: string
```
